import { ComponentMeta } from '@storybook/react'
import { {{pascalCase name}} } from '..'

export default {
  title: '{{pascalCase component_level}}/{{pascalCase name}}',
  component: {{pascalCase name}}
} as ComponentMeta<typeof {{pascalCase name}}>

export const Basic = {}
